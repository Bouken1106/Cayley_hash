{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run with Sage (sage -python)",
      "type": "python",
      "request": "launch",
      "python": "sage",
      "args": ["-python", "${file}"],
      "program": "${file}"
    }
  ]
}